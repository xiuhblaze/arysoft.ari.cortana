const defaultCSSClasses = () => {
    const BUTTON_ADD_CLASS = 'btn bg-gradient-dark text-nowrap';
    const BUTTON_SEARCH_CLASS = 'btn bg-gradient-info text-nowrap mb-0';
    const BUTTON_CLEAR_SEARCH_CLASS = 'btn btn-link text-secondary mb-0';

    return {
        BUTTON_ADD_CLASS,
        BUTTON_SEARCH_CLASS,
        BUTTON_CLEAR_SEARCH_CLASS,
    }
};

export default defaultCSSClasses;